<?xml version="1.0"?>
<ruleset name="10up PHPCS">
	<description>10up PHPCS extended.</description>

	<!-- Scan these directories -->
	<file>fixtures</file>
	<file>src</file>
	<file>tests</file>

	<!-- Use the 10up rulset -->
	<rule ref="10up-Default" />

	<rule ref="SlevomatCodingStandard.TypeHints.DeclareStrictTypes">
		<properties>
			<property name="declareOnFirstLine" value="false"/>
			<property name="spacesCountAroundEqualsSign" value="true"/>
		</properties>
	</rule>

	<!--
		Unit tests run without WordPress loaded, so WP_Filesystem is unavailable and direct
		filesystem calls are required to set up fixtures on disk. serialize() is likewise
		needed to reproduce the on-disk cache format written by older framework versions.
	-->
	<rule ref="WordPress.WP.AlternativeFunctions">
		<exclude-pattern>*/tests/*</exclude-pattern>
	</rule>
	<rule ref="WordPress.PHP.DiscouragedPHPFunctions">
		<exclude-pattern>*/tests/*</exclude-pattern>
	</rule>

	<arg value="sp"/> <!-- Show sniff and progress -->
	<arg name="colors"/> <!-- Show results with colors. Disable if working on Windows -->
	<arg name="basepath" value="."/> <!-- Strip the file paths down to the relevant bit -->
	<arg name="extensions" value="php"/> <!-- Limit to PHP -->
</ruleset>
